/*
Owner - entity which describe behaviour specific user.
 */

/*class Owner {
  readonly #accessor: AbstractAccessor;
  #id: String;
  #active: boolean;

  constructor(
    id: String,
    active: boolean,
    username: String,
    accessor: AbstractAccessor
  ) {
    this.#id = id;
    this.#active = active;
    this.#accessor = accessor;
  }

  public sync() {
    this.#accessor.connect(this.#accessor, (ctx) => {
      return "none";
    });
  }
}

export default Owner;*/
